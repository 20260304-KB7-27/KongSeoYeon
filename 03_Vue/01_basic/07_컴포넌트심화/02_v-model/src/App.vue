<template>
  <h1>v-model 테스트</h1>
  <hr />
  <BaseInput
    v-model="modelValue"
    label="닉네임"
    placeholder="닉네임을 입력해주세요"
  ></BaseInput>

  <BaseInput
    v-model:modelValue="firstname"
    label="성"
    placeholder="성을 입력해주세요"
  ></BaseInput>

  <!-- 하나의 컴포넌트에 v-model을 여러개 쓰고 싶을때 이름을 붙이 수 있다.-->
  <BaseInput
    v-model:modelValue="lastname"
    label="이름"
    placeholder="이름을 입력해주세요"
  ></BaseInput>

  <p>
    닉네임 : <strong>{{ modelValue }}</strong> 전체 이름 :
    <strong>{{ firstname }} {{ lastname }}</strong>
  </p>
</template>

<script setup>
import BaseInput from './components/BaseInput.vue';
import { ref } from 'vue';

// 입력받을 반응형데이터
const modelValue = ref('');
const firstname = ref('');
const lastname = ref('');
</script>

<style scoped></style>
